{
  "name": "com.example.app",
  "displayName": "base-phaser-cordova-project",
  "version": "1.0.0",
  "description": "Description here.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy-phaser": "npm run --prefix phaser deploy",
    "dev-phaser": "npm run --prefix phaser dev",
    "build": "npm run --prefix phaser deploy && cordova build android",
    "deploy": "npm run --prefix phaser deploy && cordova build --release android"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Pablo Pirata",
  "license": "Apache-2.0",
  "dependencies": {
    "cordova-android": "^7.1.4",
    "cordova-plugin-insomnia": "^4.3.0",
    "cordova-plugin-whitelist": "1.3.4"
  },
  "devDependencies": {
    "cordova-plugin-whitelist": "^1.3.4"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {},
      "cordova-plugin-insomnia": {}
    },
    "platforms": [
      "android"
    ]
  }
}
